# Local development overrides
# Use with: bundle exec jekyll serve --config _config.yml,_config.local.yml --livereload

# Avoid remote fetch/SSL issues during local development
remote_theme:
theme: jekyll-text-theme

plugins:
  - jekyll-feed
  - jekyll-paginate
  - jekyll-sitemap
  - jemoji
